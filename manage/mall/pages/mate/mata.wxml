<view wx:for="{{noticeList}}" data-id="{{item.id}}" catchtap="go">
<view class="name">
<view class="name_day">
<view class="item_name">{{item.title}}</view>
<view class="isread">{{item.isRead}}</view>
<view class="item_day">{{item.AddTime1}}</view>
</view>
<view class="grayLine"></view>
</view>
</view>
